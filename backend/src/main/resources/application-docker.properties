# Server Configuration
server.port=9090
server.servlet.context-path=/api

# MySQL Configuration - Using environment variables from Kubernetes
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
app.jwt.access-token-secret=a9F3kL8vQ2mZ7tR6pW1sB4xY0nC5hD3gfgffgdgdtrsrtdrddx
app.jwt.access-token-expiration=1800000

# Google Gemini AI Configuration
app.gemini.api-key=AIzaSyC59T6bVwWKy88BObCgVOOiHQwM3P3Kq_U

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.resume-dir=uploads/resumes/

# CORS Configuration
app.cors.allowed-origins=http://localhost:30080,http://localhost:3000
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.allow-credentials=true

# Logging
logging.level.com.arbeit.backend=INFO
logging.level.org.springframework.security=INFO

